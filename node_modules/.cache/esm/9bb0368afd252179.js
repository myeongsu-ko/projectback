let jwt,env;_fd2‍.x([["default",()=>_fd2‍.o]]);_fd2‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);_fd2‍.w("@root/config.json",[["default",["env"],function(v){env=v}]]);


const generateToken = (userid) => {
  const token = jwt.sign(
    // 첫번째 파라미터엔 토큰 안에 집어넣고 싶은 데이터를 넣습니다
    {
      userid: userid,
    },
    env.JWT_SECRET, // 두번째 파라미터에는 JWT 암호를 넣습니다
    {
      expiresIn: '365 days', // 1년
    },
  );
  return token;
};

_fd2‍.d(generateToken);