let Koa,Router,bodyParser,api,jwtMiddleware,env,winston;_fd2‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_fd2‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_fd2‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_fd2‍.w("./api",[["default",["api"],function(v){api=v}]]);_fd2‍.w("@lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);_fd2‍.w("@root/config.json",[["default",["env"],function(v){env=v}]]);_fd2‍.w("@root/winston",[["default",["winston"],function(v){winston=v}]]);








const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/@', api.routes());

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

// PORT 가 지정되어있지 않다면 4000 을 사용
const port = env.PORT || 4000;
app.listen(port, () => {
  winston.info(`Server Starting...  Listening to port ${port}`);
});
